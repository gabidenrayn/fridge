import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test', (WidgetTester tester) async {
    // Пустой тест — просто чтобы не было ошибок
    expect(true, isTrue);
  });
}
